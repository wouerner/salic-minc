<?php
$params = '';
if (isset($this->idPreProjeto))
    $params = "/idPreProjeto/" . $this->idPreProjeto;

if (isset($this->idPronac))
    $params = "/idPronac/" . $this->idPronac;

if($this->listarTudo == true) {
    $params .= "/listarTudo/1";
}

?>

<div class="container-fluid">
    <div class="row">
        <?php if ($this->idPreProjeto && $this->ehProponente) : ?>
            <?= $this->action('menu', 'menu', 'proposta', array('idPreProjeto' => $this->idPreProjeto)); ?>
        <?php else: ?>
            <?= $this->partial('/mensagem/partials/menu.phtml', $this); ?>
        <?php endif; ?>

        <div class="col s12 m12 l10">
            <h4>
                Solicita&ccedil;&otilde;es
            </h4>
            <?php if($this->idPronac) :  ?>
                <div class="row">
                    <div class="col s12">
                        <b>Pronac: </b><?= $this->projeto->AnoProjeto . $this->projeto->Sequencial; ?><br>
                    </div>
                    <div class="col s12">
                        <b>Título: </b><?= $this->projeto->NomeProjeto; ?>
                    </div>
                </div>
            <?php elseif($this->idPreProjeto) : ?>
                <div class="row">
                    <div class="col s12">
                        <b>Nº Proposta: </b><?= $this->proposta->idPreProjeto; ?><br>
                    </div>
                    <div class="col s12">
                        <b>Título: </b><?= $this->proposta->NomeProjeto; ?>
                    </div>
                </div>
            <?php endif; ?>

            <div id="container-list" class="container" style="width: 100%"
                 data-ajax-render='/solicitacao/mensagem/listar<?= $params; ?>'>
            </div>

            <?php if(!empty($this->existeSolicitacaoEnviadaNaoRespondida)) : ?>
                <div class="center-align padding20">
                    <a href="javascript:void(0)"
                       onload="(function(e){ alert(e); })(event)"
                       class="btn disabled">Nova Solicita&ccedil;&atilde;o</a>
                </div>
                <div class="message"><span>Você já possui uma solicitação aguardando resposta!</span></div>
            <?php elseif (isset($this->projeto->IdPRONAC) || $this->proposta->idPreProjeto): ?>
                <?php if ($this->ehProponente) : ?>
                    <div class="center-align padding20">
                        <a id="btn_enviar"
                           href="<?= $this->url(array('module' => 'solicitacao', 'controller' => 'mensagem', 'action' => 'solicitar', 'idPronac' => $this->projeto->IdPRONAC, 'idPreProjeto' => $this->proposta->idPreProjeto)); ?>"
                           class="btn waves-effect waves-light bt_rascunho">Nova Solicita&ccedil;&atilde;o</a>
                    </div>

                    <div class="fixed-action-btn animated bounceInUp pulse">
                        <a href="javascript:$3.redirect('<?= $this->url(array('module' => 'solicitacao', 'controller' => 'mensagem', 'action' => 'solicitar', 'idPronac' => $this->projeto->IdPRONAC, 'idPreProjeto' => $this->proposta->idPreProjeto)); ?>');"
                           class="btn-floating btn-large tooltipped waves-effect waves-light" data-position="left"
                           data-delay="50"
                           data-tooltip="Escrever uma solicita&ccedil;&atilde;o">
                            <i class="large material-icons">create</i>
                        </a>
                    </div>
                <?php endif; ?>
            <?php endif; ?>
        </div>
    </div>
</div>
