<?php
if (isset($this->proposta['idpreprojeto'])) {
    echo $this->action('menu', 'menu', 'proposta', array('idPreProjeto' => $this->idPreProjeto));  // menu
}

?>
<?php
    $links = array(
        $this->layout['listagem'],
        array('Local e Per&iacute;odo de Realiza&ccedil;&atilde;o' => array())
    );
gerarBreadCrumb($links);
?>

<div id="titulo">
    <div>Documentos Pendentes<?php echo montaBotaoVoltar($controller, $links); ?></div>
</div>
<div id="conteudo">
    <br />
    <table class="tabela" style="margin-bottom: 100px">

        <tr class="titulo_tabela" >
            <th width="70%">Documentos Pendentes</th>
            <th>Tipo</th>
        </tr>
        <?php $ct=0; ?>
        <?php foreach($this->documentosPendentes as $documento): ?>
        <tr class="fundo">
            <td class="centro"><?php echo $documento->Descricao; ?></td>
            <td class="centro"><?php echo ($documento->Opcao==2)?"Projeto":"Proponente"; ?></td>
        </tr>
        <?php $ct++; ?>
        <?php endforeach; ?>
        <?php if($ct==0): ?>
        <tr class="fundo">
            <td class="centro" colspan="2" style="height: 50px"><b>No Momento n&atilde;o h&aacute; pend&ecirc;ncias</b></td>
        </tr>
        <?php endif; ?>
    </table>
</div>
<br clear="all" />
<div id="rodapeConteudo"><span></span></div>
